/**
 * @author      : HackOlympus (zeus@hackolympus)
 * @file        : Soft_Drinking
 * @created     : Tuesday Feb 15, 2022 02:19:38 IST
 */

#include <bits/stdc++.h>

#define FOR(n) for (int i=0; i < n ; i++)
#define REP(I,n,a) for(int i=I; i < n; i+=a) 

using ll = long long;

using namespace std ; 

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    // main code : 
    int n, k, l, c, d, p, nl, np ; 
    cin >> n >> k >> l >> c >> d >> p >> nl >> np ; 
    int limeslices = c * d ; 
    int salt = p ; 
    int totalvol = l * k ; 
    int toast = totalvol / nl +  
    return 0 ; 
}


