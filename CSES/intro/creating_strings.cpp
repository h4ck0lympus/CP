#include "bits/stdc++.h"

using namespace std;

#define int long long

void s(string s, int i, )

void solve() {
  string s; cin >> s;

  sort(s.begin(), s.end());
  set<string> strings;

  for (int i=0; i < s.size(); i++) {

  }
}

#ifdef int
#undef int
#endif
int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  // int t;
  // cin >> t;
  // while (t--)
    solve();
}
