#include "bits/stdc++.h"

using namespace std;

typedef long long ll;

void solve() {
    int n;
    vector<pair<ll, ll>> position;
    for (int i=0; i < n; i++) {
	ll x, y; cin >> x >> y;
	position.push_back(make_pair(x,y));
    }
}

int main() {
    solve();
}
